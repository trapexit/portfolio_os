#ifndef	CLIB_H
#define	CLIB_H

/*
 * Prototypes for function in clib that are not prototyped elsewhere,
 * and some that are.
 *
 * XXX- This file should only be a waystation between not having
 * prototypes at all and having protoypes placed in the "right" place.
 */

int	isUser(void);
int	getPSR(void);

#endif
